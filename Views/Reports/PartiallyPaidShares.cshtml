﻿@using System.Security.Claims
@model SACCOBlockChainSystem.Models.ViewModels.PartiallyPaidSharesDynamicViewModel
@{
    ViewData["Title"] = "Partially Paid Shares Report";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var hasData = Model != null && Model.Members != null && Model.Members.Any();
    var reportDate = Model?.ReportDate ?? (ViewBag.ReportDate as DateTime? ?? DateTime.Now);
    var companyName = Model?.CompanyName ?? ViewBag.CompanyName ?? "";
    var shareTypes = Model?.ShareTypes ?? (ViewBag.ShareTypes as List<Sharetype> ?? new List<Sharetype>());
}

<style media="print">
    .app-sidebar, .app-header, .main-footer, .sidebar-wrapper, .navbar,
    .breadcrumb, .footer, [data-lte-toggle="sidebar"], .brand-link,
    .sidebar-footer, .no-print {
        display: none !important;
    }

    .app-main, .app-content, .content-wrapper, .main-content {
        margin-left: 0 !important;
        padding: 20px !important;
        width: 100% !important;
    }

    .card {
        border: none !important;
        box-shadow: none !important;
    }

    .card-header {
        background-color: #ffc107 !important;
        -webkit-print-color-adjust: exact;
        print-color-adjust: exact;
    }

    .table {
        width: 100% !important;
        border-collapse: collapse !important;
        font-size: 9pt !important;
    }

        .table th {
            background-color: #f2f2f2 !important;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
        }

    .btn, .export-buttons, #exportExcelBtn, #exportPdfBtn {
        display: none !important;
    }

    body {
        background: white !important;
        font-size: 9pt;
    }

    .bg-warning {
        background-color: #ffc107 !important;
        -webkit-print-color-adjust: exact;
        print-color-adjust: exact;
    }

    .table-responsive {
        overflow: visible !important;
    }
</style>

<div class="card">
    <div class="card-header bg-warning">
        <h3 class="card-title">
            <i class="fas fa-exclamation-triangle me-2"></i>Partially Paid Shares Register
        </h3>
    </div>

    <div class="card-body">
        <!-- Filter Section -->
        <div class="row mb-4 no-print">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header bg-light">
                        <h5 class="mb-0"><i class="fas fa-filter me-2"></i>Report Date</h5>
                    </div>
                    <div class="card-body">
                        <form asp-action="PartiallyPaidShares" method="post" id="reportForm">
                            <div class="row">
                                <div class="col-md-4">
                                    <div class="form-group">
                                        <label class="form-label fw-bold">As At Date</label>
                                        <input type="date" name="reportDate" id="reportDate"
                                               class="form-control"
                                               value="@reportDate.ToString("yyyy-MM-dd")"
                                               required />
                                    </div>
                                </div>
                                <div class="col-md-8">
                                    <div class="form-group">
                                        <label class="form-label fw-bold">&nbsp;</label>
                                        <div>
                                            <button type="submit" class="btn btn-primary">
                                                <i class="fas fa-search me-1"></i>Generate Report
                                            </button>
                                            <button type="button" class="btn btn-success" id="exportExcelBtn">
                                                <i class="fas fa-file-excel me-1"></i>Excel
                                            </button>
                                            <button type="button" class="btn btn-danger" id="exportPdfBtn">
                                                <i class="fas fa-file-pdf me-1"></i>PDF
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </form>

                        <!-- Export Forms -->
                        <form asp-action="ExportPartiallyPaidSharesToExcel" method="post" id="excelForm" style="display:none;">
                            <input type="hidden" name="reportDate" id="excelReportDate" />
                        </form>
                        <form asp-action="ExportPartiallyPaidSharesToPdf" method="post" id="pdfForm" style="display:none;">
                            <input type="hidden" name="reportDate" id="pdfReportDate" />
                        </form>
                    </div>
                </div>
            </div>
        </div>

        @if (hasData)
        {
            <!-- Header -->
            <div class="text-center mb-4">
                <h2 class="display-6 fw-bold">PARTIALLY PAID SHARES REPORT</h2>
                <h1 class="display-5 fw-bold text-warning">@companyName.ToUpper()</h1>
                <h5 class="text-muted">As At @reportDate.ToString("dd/MM/yyyy")</h5>
                <hr class="my-4" />
            </div>

            <!-- Summary Cards -->
            <div class="row mb-4">
                <div class="col-md-3">
                    <div class="small-box bg-info">
                        <div class="inner">
                            <h3>@Model.TotalMembers</h3>
                            <p>Partially Paid Members</p>
                        </div>
                        <div class="icon">
                            <i class="fas fa-users"></i>
                        </div>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="small-box bg-warning">
                        <div class="inner">
                            <h3>@Model.TotalAmount.ToString("N0")</h3>
                            <p>Total Contributions</p>
                        </div>
                        <div class="icon">
                            <i class="fas fa-coins"></i>
                        </div>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="small-box bg-danger">
                        <div class="inner">
                            <h3>@Model.MinimumShareRequirement.ToString("N0")</h3>
                            <p>Min Share Requirement</p>
                        </div>
                        <div class="icon">
                            <i class="fas fa-bullseye"></i>
                        </div>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="small-box bg-success">
                        <div class="inner">
                            <h3>@Model.ShareTypes.Count</h3>
                            <p>Share Types</p>
                        </div>
                        <div class="icon">
                            <i class="fas fa-tags"></i>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Requirements Summary -->
            <div class="row mb-3">
                <div class="col-md-12">
                    <div class="alert alert-warning">
                        <i class="fas fa-exclamation-triangle me-2"></i>
                        <strong>Partially Paid Members:</strong> Members who have made contributions but have NOT met the minimum share requirement of <strong>@Model.MinimumShareRequirement.ToString("N0")</strong>
                        <span class="ms-3">
                            <span class="badge bg-warning">Partially Paid</span>
                        </span>
                    </div>
                </div>
            </div>

            <!-- Members Table - Dynamic Columns -->
            <div class="table-responsive">
                <table class="table table-bordered table-striped table-hover" id="membersTable">
                    <thead class="bg-warning">
                        <tr>
                            <th>#</th>
                            <th>MemberNo</th>
                            <th>Names</th>
                            <th>Sex</th>

                            @foreach (var shareType in shareTypes)
                            {
                                <th class="text-center">@shareType.SharesType</th>
                            }
                            <th class="text-center">Total</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{
                            int serialNo = 1;
                            foreach (var member in Model.Members)
                            {
                                <tr>
                                    <td class="text-center">@(serialNo++)</td>
                                    <td>@member["MemberNo"]</td>
                                    <td>@member["FullName"]</td>
                                    <td>@member["Sex"]</td>

                                    @foreach (var shareType in shareTypes)
                                    {
                                        var prefix = shareType.SharesCode.Replace(" ", "_").Replace("-", "_").ToUpper();
                                        var amount = member.ContainsKey($"{prefix}_Amount") ? Convert.ToDecimal(member[$"{prefix}_Amount"]) : 0;
                                        var isZero = amount == 0;

                                        <td class="text-end @(isZero ? "text-danger fw-bold" : "")">
                                            @amount.ToString("N2")
                                        </td>
                                    }

                                    <td class="text-end fw-bold">@Convert.ToDecimal(member["TotalAmount"]).ToString("N2")</td>
                                </tr>
                            }
                        }
                    </tbody>
                    <tfoot class="bg-light fw-bold">
                        <tr>
                            <td colspan="4" class="text-end">GRAND TOTAL:</td>
                            @foreach (var shareType in shareTypes)
                            {
                                var prefix = shareType.SharesCode.Replace(" ", "_").Replace("-", "_").ToUpper();
                                var totalAmount = Model.Members.Sum(m => Convert.ToDecimal(m.ContainsKey($"{prefix}_Amount") ? m[$"{prefix}_Amount"] : 0));

                                <td class="text-end text-primary">@totalAmount.ToString("N2")</td>
                            }
                            <td class="text-end text-primary">@Model.TotalAmount.ToString("N2")</td>
                        </tr>
                    </tfoot>
                </table>
            </div>

            <!-- Statistics Section -->
            <div class="row mt-4">
                <div class="col-md-4">
                    <div class="card">
                        <div class="card-header bg-light">
                            <h5 class="mb-0"><i class="fas fa-venus-mars me-2"></i>Member Demographics</h5>
                        </div>
                        <div class="card-body">
                            <table class="table table-sm">
                                <tr>
                                    <td width="60%"><strong>Total Members:</strong></td>
                                    <td class="text-end"><strong>@Model.TotalMembers</strong></td>
                                </tr>
                                <tr>
                                    <td>Male:</td>
                                    <td class="text-end">@Model.MaleCount (@((Model.MaleCount * 100 / (Model.TotalMembers == 0 ? 1 : Model.TotalMembers)).ToString("N0"))%)</td>
                                </tr>
                                <tr>
                                    <td>Female:</td>
                                    <td class="text-end">@Model.FemaleCount (@((Model.FemaleCount * 100 / (Model.TotalMembers == 0 ? 1 : Model.TotalMembers)).ToString("N0"))%)</td>
                                </tr>
                                <tr>
                                    <td>Other:</td>
                                    <td class="text-end">@Model.OtherCount (@((Model.OtherCount * 100 / (Model.TotalMembers == 0 ? 1 : Model.TotalMembers)).ToString("N0"))%)</td>
                                </tr>
                            </table>
                        </div>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card">
                        <div class="card-header bg-light">
                            <h5 class="mb-0"><i class="fas fa-chart-pie me-2"></i>Payment Summary</h5>
                        </div>
                        <div class="card-body">
                            <table class="table table-sm">
                                <tr>
                                    <td width="60%"><strong>Members with Zero Share Capital:</strong></td>
                                    <td class="text-end"><strong>@Model.MembersWithZeroShareCapital</strong></td>
                                </tr>
                                <tr>
                                    <td><strong>Members with Zero Savings/Deposits:</strong></td>
                                    <td class="text-end"><strong>@Model.MembersWithZeroSavings</strong></td>
                                </tr>
                                <tr>
                                    <td><strong>Members with Zero Registration Fee:</strong></td>
                                    <td class="text-end"><strong>@Model.MembersWithZeroRegFee</strong></td>
                                </tr>
                            </table>
                        </div>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card">
                        <div class="card-header bg-light">
                            <h5 class="mb-0"><i class="fas fa-chart-line me-2"></i>Financial Summary</h5>
                        </div>
                        <div class="card-body">
                            <table class="table table-sm">
                                <tr>
                                    <td width="60%"><strong>Avg Contribution:</strong></td>
                                    <td class="text-end"><strong>@((Model.TotalAmount / (Model.TotalMembers == 0 ? 1 : Model.TotalMembers)).ToString("N2"))</strong></td>
                                </tr>
                                <tr>
                                    <td><strong>Min Share Requirement:</strong></td>
                                    <td class="text-end"><strong>@Model.MinimumShareRequirement.ToString("N2")</strong></td>
                                </tr>
                                <tr>
                                    <td><strong>Share Types:</strong></td>
                                    <td class="text-end"><strong>@Model.ShareTypes.Count</strong></td>
                                </tr>
                            </table>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Share Types Legend -->
            <div class="row mt-3">
                <div class="col-12">
                    <div class="card">
                        <div class="card-header bg-light">
                            <h5 class="mb-0"><i class="fas fa-tags me-2"></i>Share Types Legend</h5>
                        </div>
                        <div class="card-body">
                            <div class="row">
                                @foreach (var shareType in shareTypes)
                                {
                                    <div class="col-md-3 col-sm-6 mb-2">
                                        <span class="badge bg-warning me-1">@shareType.SharesCode</span>
                                        <span>@shareType.SharesType</span>
                                        @if (shareType.IsMainShares)
                                        {
                                            <span class="badge bg-info ms-1">Main</span>
                                        }
                                        @if (shareType.MinAmount > 0)
                                        {
                                            <span class="badge bg-secondary ms-1">Min: @shareType.MinAmount.ToString("N0")</span>
                                        }
                                    </div>
                                }
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Report Information -->
            <div class="row mt-3">
                <div class="col-12">
                    <div class="small text-muted">
                        <i class="fas fa-info-circle"></i>
                        Generated By: @User.Identity.Name on @DateTime.Now.ToString("dd/MM/yyyy HH:mm:ss") |
                        As At: @Model.ReportDate.ToString("dd/MM/yyyy") |
                        Company: @companyName |
                        Total Records: @Model.TotalMembers |
                        Share Types: @shareTypes.Count
                    </div>
                </div>
            </div>
        }
        else
        {
            <div class="alert alert-success text-center py-5">
                <i class="fas fa-check-circle fa-3x mb-3 text-success"></i>
                <h5>No Partially Paid Members Found</h5>
                <p>All active members have fully paid their shares as at @reportDate.ToString("dd/MM/yyyy")</p>
                <p class="text-muted small">Note: Only members with at least one contribution are considered.</p>
            </div>
        }
    </div>
</div>

@section Scripts {
    <script>
        (function() {
            'use strict';

            function domReady(fn) {
                if (document.readyState === 'loading') {
                    document.addEventListener('DOMContentLoaded', fn);
                } else {
                    fn();
                }
            }

            domReady(function() {
                // Export to Excel
                const exportExcelBtn = document.getElementById('exportExcelBtn');
                const exportPdfBtn = document.getElementById('exportPdfBtn');
                const reportDate = document.getElementById('reportDate');
                const excelReportDate = document.getElementById('excelReportDate');
                const pdfReportDate = document.getElementById('pdfReportDate');
                const excelForm = document.getElementById('excelForm');
                const pdfForm = document.getElementById('pdfForm');
                const reportForm = document.getElementById('reportForm');
                const membersTable = document.getElementById('membersTable');

                if (exportExcelBtn) {
                    exportExcelBtn.addEventListener('click', function(e) {
                        e.preventDefault();
                        e.stopPropagation();

                        if (!reportDate) {
                            alert('Report date input not found');
                            return;
                        }

                        const date = reportDate.value;
                        if (!date) {
                            alert('Please select a report date');
                            return;
                        }

                        if (!excelReportDate || !excelForm) {
                            alert('Export form not properly configured');
                            return;
                        }

                        const originalText = exportExcelBtn.innerHTML;
                        exportExcelBtn.innerHTML = '<i class="fas fa-spinner fa-spin me-1"></i>Exporting...';
                        exportExcelBtn.disabled = true;

                        excelReportDate.value = date;
                        excelForm.submit();

                        setTimeout(function() {
                            exportExcelBtn.innerHTML = originalText;
                            exportExcelBtn.disabled = false;
                        }, 3000);
                    });
                }

                if (exportPdfBtn) {
                    exportPdfBtn.addEventListener('click', function(e) {
                        e.preventDefault();
                        e.stopPropagation();

                        if (!reportDate) {
                            alert('Report date input not found');
                            return;
                        }

                        const date = reportDate.value;
                        if (!date) {
                            alert('Please select a report date');
                            return;
                        }

                        if (!pdfReportDate || !pdfForm) {
                            alert('Export form not properly configured');
                            return;
                        }

                        const originalText = exportPdfBtn.innerHTML;
                        exportPdfBtn.innerHTML = '<i class="fas fa-spinner fa-spin me-1"></i>Exporting...';
                        exportPdfBtn.disabled = true;

                        pdfReportDate.value = date;
                        pdfForm.submit();

                        setTimeout(function() {
                            exportPdfBtn.innerHTML = originalText;
                            exportPdfBtn.disabled = false;
                        }, 3000);
                    });
                }

                // Handle report form submission
                if (reportForm) {
                    reportForm.addEventListener('submit', function(e) {
                        if (!reportDate) return;

                        const date = reportDate.value;
                        if (!date) {
                            e.preventDefault();
                            alert('Please select a report date');
                            return false;
                        }
                    });
                }

                // Initialize DataTable
                @if (hasData)
                {
                            <text>
                            if (membersTable && typeof $.fn !== 'undefined' && $.fn.DataTable) {
                                try {
                                    $(membersTable).DataTable({
                                        pageLength: 25,
                                        ordering: true,
                                        responsive: true,
                                        scrollX: true,
                                        language: {
                                            search: "Search:",
                                            lengthMenu: "Show _MENU_ entries",
                                            info: "Showing _START_ to _END_ of _TOTAL_ entries"
                                        }
                                    });
                                } catch (error) {
                                    console.error('DataTable initialization error:', error);
                                }
                            }
                            </text>
                }

                // Auto-focus
                if (reportDate) {
                    reportDate.focus();
                }
            });
        })();
    </script>
}

<style>
    .small-box {
        border-radius: 0.25rem;
        box-shadow: 0 0 1px rgba(0,0,0,.125), 0 1px 3px rgba(0,0,0,.2);
        display: block;
        margin-bottom: 20px;
        position: relative;
    }

        .small-box > .inner {
            padding: 10px;
        }

        .small-box h3 {
            font-size: 2.2rem;
            font-weight: 700;
            margin: 0 0 10px;
            padding: 0;
            white-space: nowrap;
            color: white;
        }

        .small-box p {
            color: white;
            font-size: 1rem;
            margin: 0;
        }

        .small-box .icon {
            color: rgba(0,0,0,0.15);
            position: absolute;
            right: 10px;
            top: 10px;
            transition: all .3s linear;
            z-index: 0;
        }

            .small-box .icon > i {
                font-size: 70px;
            }

        .small-box:hover .icon {
            font-size: 75px;
        }

    .bg-warning {
        background-color: #ffc107 !important;
    }

    .text-warning {
        color: #ffc107 !important;
    }

    .badge.bg-warning {
        background-color: #ffc107 !important;
    }

    @@media print {
        .table-responsive {
            overflow: visible !important;
        }

        .table {
            font-size: 8pt !important;
        }

            .table th, .table td {
                padding: 3px !important;
            }
    }

    @@media (max-width: 767px) {
        .small-box {
            text-align: center;
        }

            .small-box .icon {
                display: none;
            }
    }
</style>